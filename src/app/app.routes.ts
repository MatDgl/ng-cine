import { Routes } from '@angular/router';
import { WishComponent } from './components/wish/wish.component';

export const routes: Routes = [
  { path: 'wish', component: WishComponent }, // Envies de voir
];
